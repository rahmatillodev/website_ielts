# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
# Catch-all: any .env* variant holds secrets and must never be committed.
.env*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Content-audit working artifacts.
# These hold production test UUIDs and question answer keys - this repo is
# public, so they must never be committed.
AUDIT_OPEN_ITEMS.md
CONTENT_AUDIT_LOG.md
PHASE_B_REPORT.md
phaseB_findings.json
content_audit_changes.json
coverage_plan.csv
DELETED_*.json
TRANSCRIPTION_PROGRESS.md
scripts/transcribe/transcription_state.json
scripts/transcribe/audio_inventory.json
scripts/transcribe/transcripts/
